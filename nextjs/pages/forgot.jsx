/** @jsxImportSource @emotion/react */
import Head from "next/head";
import { css } from "@emotion/react";
import LayoutAuth from "@/containers/LayoutAuth/LayoutAuth";
import {InputEmail,Button,H2,Space} from "@/components";
import FormControl from "@/containers/FormControl/FormControl";
import { useState } from "react";

export default function Forgot() {
  const mainDivMainPage = css`
      width:100%;
      height:100%;
      text-align:center;
    `;

  const [formValue,setFormValue] = useState({})

  const handleSubmit=(event)=>{
    if(event){
      event.preventDefault()
    }
    console.log("login page on submit",formValue)
  }

  const handleOnChange = (name,value) =>{
    setFormValue({
      ...formValue,
      [name]:value,
    })
  }


  return (
    <>
      <Head>
        <title>فراموشی رمز عبور</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main css={mainDivMainPage}>
        <LayoutAuth>
          <Space/>
          <H2 align={"center"}>بازنشانی رمز عبور با ایمیل</H2>
          <form onSubmit={handleSubmit}>
            <FormControl>
              <InputEmail onChange={(value)=> handleOnChange('email',value)} type="email" placeholder="ایمیل"/>
            </FormControl>
            <FormControl>
              <Button type="submit">ارسال</Button>
            </FormControl>
          </form>
        </LayoutAuth>
      </main>
    </>
  );
}
